/** Number of events in the daily Weave puzzle. */
export const DAILY_WEAVE_EVENT_COUNT = 7;

/** Chronological index (0-based) pre-placed in the matching slot — center of seven (slot 4). */
export const AUTOPLACED_CHRONOLOGY_INDEX = 3;

/** Legacy tries cap kept for backward compatibility when reading old stored results. */
export const MAX_TRIES = 5;
