/** Number of events in the daily Weave puzzle. */
export const DAILY_WEAVE_EVENT_COUNT = 5;

/** Chronological index (0-based) pre-placed in the matching slot — center of five (slot 3). */
export const AUTOPLACED_CHRONOLOGY_INDEX = 2;

/** Wrong-drop attempts allowed before lockout. */
export const MAX_TRIES = 5;
